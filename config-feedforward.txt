# NEAT Configuration for Flappy Bird AI - OPTIMIZED
# ==================================================
# Tuned for fast convergence and stable learning

[NEAT]
fitness_criterion     = max
fitness_threshold     = 300
pop_size              = 20
reset_on_extinction   = False
no_fitness_termination = False

[DefaultGenome]
# Node activation options - simplified for stability
activation_default      = tanh
activation_mutate_rate  = 0.05
activation_options      = tanh

# Node aggregation options - keep simple
aggregation_default     = sum
aggregation_mutate_rate = 0.0
aggregation_options     = sum

# Structural mutation options
single_structural_mutation = false
structural_mutation_surer = default

# Node bias options - reduced for stability
bias_init_mean          = 0.0
bias_init_stdev         = 0.5
bias_init_type          = gaussian
bias_max_value          = 5.0
bias_min_value          = -5.0
bias_mutate_power       = 0.3
bias_mutate_rate        = 0.4
bias_replace_rate       = 0.1

# Genome compatibility options - tighter grouping
compatibility_disjoint_coefficient = 1.0
compatibility_weight_coefficient   = 0.6

# Connection add/remove rates - conservative
conn_add_prob           = 0.2
conn_delete_prob        = 0.05

# Connection enable options
enabled_default         = True
enabled_mutate_rate     = 0.01
enabled_rate_to_true_add = 0.0
enabled_rate_to_false_add = 0.0

# Network topology
feed_forward            = True
initial_connection      = full_direct

# Node add/remove rates - very conservative
node_add_prob           = 0.05
node_delete_prob        = 0.02

# Network parameters
num_hidden              = 0
num_inputs              = 4
num_outputs             = 1

# Node response options - stable values
response_init_mean      = 1.0
response_init_stdev     = 0.2
response_init_type      = gaussian
response_max_value      = 3.0
response_min_value      = -3.0
response_mutate_power   = 0.2
response_mutate_rate    = 0.1
response_replace_rate   = 0.05

# Connection weight options - moderate mutation
weight_init_mean        = 0.0
weight_init_stdev       = 1.0
weight_init_type        = gaussian
weight_max_value        = 5.0
weight_min_value        = -5.0
weight_mutate_power     = 0.4
weight_mutate_rate      = 0.6
weight_replace_rate     = 0.1

[DefaultSpeciesSet]
compatibility_threshold = 3.5

[DefaultStagnation]
species_fitness_func = max
max_stagnation       = 8
species_elitism      = 2

[DefaultReproduction]
elitism            = 2
survival_threshold = 0.25
min_species_size   = 2